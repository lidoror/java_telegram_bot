pipeline {
    agent any

    parameters { string(name: 'JBOT_IMAGE_NAME', defaultValue: 'None', description: '') }

    stages('Deploy') {
        steps{
            sh 'echo $JBOT_IMAGE_NAME'

        }

    }
}